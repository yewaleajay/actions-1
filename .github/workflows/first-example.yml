name: my first work flow

on: push

jobs:
    first_jobs:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "my first github action job"
        - name: List files
          run: ls
        - name: Read file
          run: cat README.MD
          
