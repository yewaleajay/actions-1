name: My Second workflow
on: push
jobs:
    first_jobs:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4

        - name: Welcome message
          run: echo "my first github action job"

        - name: List files
          run: ls

        - name: Read file
          run: cd .. && cat README.md
